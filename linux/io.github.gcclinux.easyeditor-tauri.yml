id: io.github.gcclinux.EasyEditor
runtime: org.freedesktop.Platform
runtime-version: '24.08'
sdk: org.freedesktop.Sdk
sdk-extensions:
  - org.freedesktop.Sdk.Extension.rust-stable
  - org.freedesktop.Sdk.Extension.node20
command: easyeditor
rename-icon: io.github.gcclinux.EasyEditor
finish-args:
  - --share=ipc
  - --socket=x11
  - --socket=wayland
  - --socket=pulseaudio
  - --share=network
  - --device=dri
  - --filesystem=host
  - --talk-name=org.freedesktop.Notifications
build-options:
  append-path: /usr/lib/sdk/rust-stable/bin:/usr/lib/sdk/node20/bin
  env:
    CARGO_HOME: /run/build/EasyEditor/cargo
    RUST_BACKTRACE: '1'
modules:
  - name: EasyEditor
    buildsystem: simple
    build-commands:
      # Install Node.js dependencies
      - npm ci
      # Build the frontend
      - npm run build
      # Build Tauri app
      - npm run tauri build
      # Install the binary
      - install -Dm755 src-tauri/target/release/easyeditor /app/bin/easyeditor
      # Install desktop file
      - install -Dm644 linux/io.github.gcclinux.EasyEditor.desktop /app/share/applications/io.github.gcclinux.EasyEditor.desktop
      # Install icon
      - install -Dm644 public/easyeditor128.png /app/share/icons/hicolor/128x128/apps/io.github.gcclinux.EasyEditor.png
      # Install metainfo
      - install -Dm644 linux/io.github.gcclinux.EasyEditor.metainfo.xml /app/share/metainfo/io.github.gcclinux.EasyEditor.metainfo.xml
    sources:
      - type: git
        url: https://github.com/gcclinux/EasyEditor.git
        tag: 1.5.1